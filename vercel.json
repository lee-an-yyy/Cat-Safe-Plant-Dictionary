{
  "buildCommand": "npm ci && npx expo export --platform web",
  "outputDirectory": "expo-app/dist",
  "rootDirectory": "expo-app",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}